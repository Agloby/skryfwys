# iOS Integration

The repository reserves `apps/ios` for a future Share Extension and keyboard foundation. iOS constraints around secure fields, limited context, and full-access permissions must be documented and tested before those clients are considered production-ready.

